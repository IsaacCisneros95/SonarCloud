import {
  sum,
  sum2,
  sum3,
  sum4,
  sum5,
  sum6,
  sum7,
  sum8,
  sum9,
  sum10,
  sum11,
} from "../lib/utils";

describe("when calling sum", () => {
  it("returns the sum of the two number", () => {
    const result = sum(2, 2);

    expect(result).toBe(4);
  });
});

describe("when calling sum2", () => {
  it("returns the sum of the two number", () => {
    const result = sum2(2, 2);

    expect(result).toBe(4);
  });
});

describe("when calling sum3", () => {
  it("returns the sum of the two number", () => {
    const result = sum3(2, 2);

    expect(result).toBe(4);
  });
});

describe("when calling sum4", () => {
  it("returns the sum of the two number", () => {
    const result = sum4(2, 2);

    expect(result).toBe(4);
  });
});

describe("when calling sum5", () => {
  it("returns the sum of the two number", () => {
    const result = sum5(2, 2);

    expect(result).toBe(4);
  });
});

describe("when calling sum6", () => {
  it("returns the sum of the two number", () => {
    const result = sum6(2, 2);

    expect(result).toBe(4);
  });
});
describe("when calling sum7", () => {
  it("returns the sum of the two number", () => {
    const result = sum7(2, 2);

    expect(result).toBe(4);
  });
});
describe("when calling sum8", () => {
  it("returns the sum of the two number", () => {
    const result = sum8(2, 2);

    expect(result).toBe(4);
  });
});
// describe("when calling sum9", () => {
//   it("returns the sum of the two number", () => {
//     const result = sum9(2, 2);

//     expect(result).toBe(4);
//   });
// });
// describe("when calling sum10", () => {
//   it("returns the sum of the two number", () => {
//     const result = sum10(2, 2);

//     expect(result).toBe(4);
//   });
// });
// describe("when calling sum11", () => {
//   it("returns the sum of the two number", () => {
//     const result = sum11(2, 2);

//     expect(result).toBe(4);
//   });
// });
